{
    "name": "vscode-w3cvalidation",
    "displayName": "W3C Validation",
    "description": "Adds W3C validation support to Visual Studio Code.",
    "version": "0.0.1",
    "publisher": "Umoxfo",
    "author": {
        "name": "Makoto Sakaguchi"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Umoxfo/vscode-w3cvalidation.git"
    },
    "bugs": {
        "url": "https://github.com/Umoxfo/vscode-w3cvalidation/issues"
    },
    "license": "MIT",
    "categories": [
        "Languages",
        "Linters"
    ],
    "engines": {
        "vscode": "^1.19.0"
    },
    "activationEvents": [
        "*"
    ],
    "main": "./client/out/extension",
    "scripts": {
        "postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
        "compile:client": "tsc -p ./client/tsconfig.json --locale ja",
        "compile:server": "tsc -p ./server/tsconfig.json --locale ja",
        "compile": "npm run compile:client && npm run compile:server",
        "vscode:prepublish": "cd client && npm run update-vscode && cd .. && npm run compile"
    },
    "devDependencies": {
        "typescript": "^2.7.2",
        "tslint": "^5.9.1",
        "@types/node": "7.0.53"
    }
}
